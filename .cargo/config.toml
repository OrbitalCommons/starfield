[alias]
# Build and run the catalog stats tool
stats = "run -p starfield-core --bin catalog_stats --"

# For backwards compatibility with former bin name
star_catalog_stats = "run -p starfield-core --bin catalog_stats --"